// const { unstable_renderSubtreeIntoContainer } = require("react-dom");

// export const greetUser = user => users.map(user => console.log(`Hi ${user}`)); //named export, can have multiple per module
// // export default greetUser; //only 1 export default per module/file, doesnt need curly braces while importing

// const add = (a,b) => a+b;
// const sub = (a,b) => a-b;

// export default {
//     add,
//     sub
// };